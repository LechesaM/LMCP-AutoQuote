export const rfqRules=["Include Supply & Delivery only","Exclude medical consumables","Exclude IT equipment","Exclude catering","Exclude petrol, diesel, fuel","Exclude compulsory briefing-session tenders","Minimum estimated profit: R30,000","Minimum margin: 25%"];
export function isEligibleMockTender(tender){const text=`${tender.title||""} ${tender.description||""}`.toLowerCase();const excluded=["medical consumables","it equipment","catering","petrol","diesel","fuel"];if(excluded.some(k=>text.includes(k)))return false;if(tender.compulsoryBriefing)return false;if((tender.estimatedProfit||0)<30000)return false;if((tender.margin||0)<25)return false;return true}
