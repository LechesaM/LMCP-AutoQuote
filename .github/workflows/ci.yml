name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  frontend-and-autoquote:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: etenders_acquisition/lmcp-dashboard
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: etenders_acquisition/lmcp-dashboard/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Verify AutoQuote libraries
        run: npm run check:autoquote-libraries

      - name: Build frontend
        run: npm run build
