apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range-placeholder
# placeholder markers: requests.cpu requests.memory limits.cpu limits.memory
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 512Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
