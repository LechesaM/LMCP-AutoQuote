# Governance Docs

This folder contains governance-specific migration documentation for the `lmcp-core` target structure.

Current purpose:

- record which live modules are governance-owned
- capture coupling that blocks immediate source relocation
- define the future extraction order for governance runtime assets

Current rule:

- documentation may be centralized here
- live backend modules remain in `app/` until compatibility shims exist and the runtime surface is proven stable
