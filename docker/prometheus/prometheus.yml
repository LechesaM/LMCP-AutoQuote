global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "lmcp-backend"
    metrics_path: /observability/prometheus
    static_configs:
      - targets: ["backend:8000"]
