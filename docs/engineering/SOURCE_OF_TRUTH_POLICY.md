# Source of Truth Policy

Before any agent modifies LMCP application code, the work package must identify a source branch/ref that is both current and reviewable.

Roadmap memory, chat history, generated evidence summaries, or a stale release branch cannot substitute for the current source tree.

When local and remote states diverge, synchronisation and reconciliation are an engineering prerequisite, not administrative overhead.
