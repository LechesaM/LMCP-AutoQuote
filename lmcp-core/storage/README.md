# Storage

`lmcp-core/storage/` will own future storage-layer contracts and persistence boundaries.

Responsibility:

- database access abstractions
- file-backed persistence conventions
- storage schema ownership by domain
- migration-safe persistence interfaces

Current status:

- placeholder structure only
- current storage paths and database wiring remain unchanged

