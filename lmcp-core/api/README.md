# API

`lmcp-core/api/` will own the future canonical API surface for LMCP.

Responsibility:

- public and internal HTTP API entrypoints
- router composition
- transport contracts
- compatibility boundaries between frontend, workers, and services

Current status:

- placeholder structure only
- the current `app.main:app` runtime remains authoritative

