# Phase 31/32 Evidence Inventory

Generated during recovery audit on `recovery/rfq-quote-pack-bridge` at `c02c0d56`.

## Sources Inspected

- Current HEAD: `recovery/rfq-quote-pack-bridge`
- Tags: `v2.0-operational-baseline`, `recovery-2026-07-15`, `integration-v1.2-enterprise-base`
- Branch: `origin/release/v1.1` at `d42f8200`
- Bundle: `/Users/Shared/LMCP-Recovery-2026-07-15/LMCP-AutoQuote-all-history.bundle` verified OK and contains the release/integration/recovered refs.
- Preserved copy: `/Users/Shared/LMCP-Recovery-2026-07-15/current-project`
- Archives checked read-only:
  - `/Users/Shared/LMCP-Recovery-2026-07-15/current-project/LMCP_Backup_20260327_0451.tar.gz`
  - `/Users/Shared/LMCP-Recovery-2026-07-15/runtime-preservation/runtime/backups/LMCP-AutoQuote-Backup-20260704_020000.tar.gz`

No entire branch was merged. No runtime data was modified.

## High-Level Finding

There is no verified source evidence for a literal active route named `/rfq-work-items` or `/work-items/{rfq_id}` in the inspected refs. The strongest historical equivalent is `origin/release/v1.1:app/api/operator_workflow_routes.py`, which exposes `/operations/rfqs` and `/operations/rfqs/{tender_id}` and builds RFQ work-item detail through `app/api/operator_workflow_contracts.py`.

Current HEAD has newer RFQ lifecycle, document acquisition, BOQ, quote compilation, manual pricing, submission gate, and proof/audit services. The historical `/operations` work-item bridge is missing from current HEAD and would not be safe to copy wholesale because it depends on older or absent helpers and on release-branch `live_rfq_store.py` intelligence functions.

## Capability Evidence

| Capability | Current status | Current file(s) | Historical file(s) / source | Assessment | Confidence | Overwrite risk |
|---|---|---|---|---|---|---|
| Canonical RFQ/work-item synchronization | Partial. Current has `rfq_lifecycle` state and stable RFQ APIs, but no single canonical work-item facade. | `app/api/rfq_lifecycle_api.py`, `app/services/rfq_lifecycle_service.py`, `app/services/rfq_state_store.py`, `app/api/rfq_stable_api.py`, `app/api/quotes_stable_api.py` | `origin/release/v1.1:app/api/operator_workflow_routes.py`, `origin/release/v1.1:app/api/operator_workflow_contracts.py` | Present but disconnected from frontend work-item model. Historical equivalent uses `/operations/rfqs`, not `/rfq-work-items`. | HIGH | HIGH if old live store or lifecycle code is copied over current code. |
| Command Centre bridge | Partial. Current has command wall/dashboard services, but no `/operations` workflow bridge. | `frontend/src/services/commandWallApi.js`, `app/api/dashboard.py`, `app/api/mission_control_compat_api.py` | `origin/release/v1.1:app/api/operator_workflow_routes.py`, `origin/release/v1.1:app/api/operator_ops_routes.py`, `origin/release/v1.1:app/api/router_registry.py` includes operator workflow/ops routes. | Backend route missing; frontend command wall remains generic. | HIGH | MEDIUM. Route addition can be additive if dependencies are adapted. |
| Canonical work-item route or equivalent | Missing route. No current `/rfq-work-items`, `/work-items/{rfq_id}`, or `/operations/rfqs` router. | No active route found in current `app/main.py` or `app/api`. | `origin/release/v1.1:app/api/operator_workflow_routes.py` defines `/operations/rfqs`, `/operations/rfqs/{tender_id}`. | Missing source/API surface in current. | HIGH | MEDIUM if restored as additive read-only facade; HIGH if copied wholesale. |
| Expanded RFQ detail/execution workspace | Frontend partially present; backend historical equivalent missing. | `frontend/src/components/RfqOperationsWorkspace.jsx`; current fetches `/api/rfq/recent`, `/submission-history/recent`, `/submission-proof/latest`, `/portal-submission/status`; detail drawer supports docs, BOQ, pricing, proof, quote pack. | `origin/release/v1.1:frontend/src/components/RfqOperationsWorkspace.jsx` fetched `/operations/rfqs`; `origin/release/v1.1:app/api/operator_workflow_contracts.py` builds detail fields. | Frontend capability is newer but disconnected from the historical work-item API. | HIGH | MEDIUM for endpoint binding; LOW if frontend is not modified in this phase. |
| Buyer document and returnable binding | Backend present in newer services, but current Live RFQ records lack fields. | `app/services/rfq_document_acquisition_engine.py`, `app/services/rfq_document_intelligence.py`, `app/api/quotes_stable_api.py`, `app/services/tender_harvester.py` | `origin/release/v1.1:app/services/live_rfq_store.py` had `summarize_rfq_document_intelligence`; `operator_workflow_contracts.py` consumed it. | Functionality exists but is not bound into current live RFQ/work-item rows. | MEDIUM | HIGH if replacing `live_rfq_store.py`; LOW if adding adapter. |
| Real document acquisition and recovery | Present and newer. | `app/services/rfq_document_acquisition_engine.py`, `app/services/etenders_*`, `app/services/rfq_recovery_service.py`, `app/services/rfq_lifecycle_service.py` | `origin/release/v1.1` also has acquisition-backed validation and RFQ lifecycle integrations. | Present, likely supersedes older code. | HIGH | HIGH if downgraded. |
| BOQ extraction and editable pricing rows | Partial. BOQ services and manual pricing UI exist; data binding is weak due stale RFQs. | `app/services/rfq_boq_extraction_engine.py`, `app/services/boq_*`, `frontend/src/components/RfqOperationsWorkspace.jsx`, `/rfq-lifecycle/manual-pricing/{rfq_id}` references in frontend | `origin/release/v1.1:frontend/src/components/RfqOperationsWorkspace.jsx` has manual pricing and document status additions; release tests include BOQ services. | Present but blocked by stale/missing record fields and possibly missing route coverage. | MEDIUM | MEDIUM. |
| Pending table-parse recovery | Present in newer document/BOQ services, current data not proving active. | `app/services/rfq_boq_extraction_engine.py`, `app/services/rfq_zip_content_extraction_engine.py`, `app/services/rfq_docx_main_document_intelligence.py` | `origin/release/v1.1` contains similar services and tests. | Present but runtime evidence inconclusive. | MEDIUM | MEDIUM. |
| Pricing-ready queue alignment | Partial. Lifecycle has `DOCUMENTS_PARSED -> PRICED -> QUOTE_PACK_READY`; frontend computes quote readiness. | `app/services/rfq_lifecycle_service.py`, `frontend/src/components/RfqOperationsWorkspace.jsx` | Historical `/operations` detail computed pricing evidence and workflow rows. | Present but no canonical queue route aligned to frontend work-items. | MEDIUM | MEDIUM. |
| Buyer form completion/review/approval gates | Present in current via form and submission gate services; historical operator workflow adds review-ready bundle. | `app/services/buyer_form_population_service.py`, `app/services/buyer_pdf_renderer_form_filler_service.py`, `app/api/quote_compilation_api.py` submission-gate routes | `origin/release/v1.1:app/services/submission_quality_service.py`, `operator_workflow_contracts.py` | Present but not unified into work-item detail. | MEDIUM | MEDIUM. |
| Quote pack generation | Present and active. | `app/services/quote_pack_service.py`, `app/services/quote_pack_v44_service.py`, `app/api/quote_pack_v44_api.py`, `app/api/quote_compilation_api.py` | Same in all inspected refs; release branch also references quote pack generation in workflow route. | Present and newer/current. | HIGH | HIGH if replaced. |
| Submission pack generation | Present in current as V45 and quote-compilation submission gates; release branch has additional package service absent in current. | `app/services/submission_pack_v45_service.py`, `app/services/submission_pack_assembler_service.py`, `app/api/submission_pack_v45_api.py`, `app/api/quote_compilation_api.py` | `origin/release/v1.1:app/services/submission_package_service.py`, `operator_workflow_routes.py` | Present, but historical operator route package facade missing. | HIGH | HIGH if replacing current submission pack services. |
| Operator execution state | Partial/missing historical facade. Current has operator action/auth services; release branch had submission execution service. | `app/api/operator_actions_api.py`, `app/services/operator_action_service.py`, `app/services/operator_auth_service.py` | `origin/release/v1.1:app/services/submission_execution_service.py`, `operator_workflow_contracts.py` | Current has operator services but not the historical per-RFQ execution state object. | HIGH | MEDIUM. |
| Dashboard count alignment | Partial and stale-data blocked. | `app/api/dashboard.py`, `app/api/revenue_dashboard_api.py`, `app/api/mission_control_compat_api.py` | `origin/release/v1.1:app/api/mission_control_snapshot_api.py`, `operator_workflow_contracts.py` | Current counts depend on stale/empty live data. | HIGH | LOW for adapter; HIGH for broad replacement. |
| Frontend proxy/API binding | Partial. Current Vite workspace fetches current stable/historical APIs, not `/operations/rfqs`. | `frontend/src/components/RfqOperationsWorkspace.jsx`, `frontend/src/services/api.js` | Release branch RfqOperationsWorkspace fetched `/operations/rfqs`; backup tar contains frontend copies only. | Binding changed/disconnected. | HIGH | MEDIUM. |
| Manual submission safety controls | Present and must remain. | `app/api/quote_compilation_api.py`, `app/services/quote_compilation_service.py`, `app/services/final_submission_v47_5_service.py`, `frontend/src/components/RfqOperationsWorkspace.jsx` | Release branch also has `submission_execution_service.py`, physical/governance services. | Present; do not weaken. | HIGH | HIGH if old automation policy copied. |
| Submission proof and audit integration | Present. | `app/api/quote_compilation_api.py`, `app/services/quote_compilation_service.py`, `app/api/proof_center_api.py`, `app/api/audit_trail_api.py` | Release branch has additional submission proof/reconciliation/receipt services. | Present and likely newer. Historical pieces may be additive only. | HIGH | HIGH if replaced. |

## Important Negative Evidence

- `git grep` across `HEAD`, `v2.0-operational-baseline`, `recovery-2026-07-15`, `integration-v1.2-enterprise-base`, and `origin/release/v1.1` found no literal `rfq-work-items` route.
- Current `app/main.py` includes `quote_compilation_router`, `supply_command_api`, `rfq_lifecycle_router`, and `mission_control_compat_api`, but not `operator_workflow_routes`.
- `origin/release/v1.1:app/api/router_registry.py` includes `operator_ops_router` and `operator_workflow_router`.
- Current tree lacks release-branch dependencies required by the historical operator workflow route, including `app/core/runtime_paths.py`, `app/domain/workflow.py`, `app/qualification/qualification_engine.py`, and the release-branch submission package/quality/execution helpers.

## Evidence-Based Conclusion

Phase 31/32 functionality is not wholly absent. Core operational engines are present in newer form, but the canonical RFQ/work-item bridge and historical `/operations/rfqs` surface are missing or disconnected. The safest first recovery target is an additive, read-only canonical work-item route or adapter backed by current `rfq_lifecycle`, `quote_compilation`, and live RFQ data, using the release branch only as a field/contract reference.
