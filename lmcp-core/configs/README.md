# Configs

`lmcp-core/configs/` will own future platform configuration definitions.

Responsibility:

- environment templates
- deployment profiles
- service-level configuration contracts
- migration-safe configuration defaults

Current status:

- placeholder structure only
- current root-level environment and compose configuration remains authoritative

